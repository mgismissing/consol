echo CONSOL HELP ^| INSTALL ^| MAKE name ^| UPDATE name ^| CLEAR ^| code conf
echo ^ ^ CLEAR         Clears the consol's cache completely.
echo ^ ^ code          Indicates the code file's name.
echo ^ ^ conf          Indicates the conf file's name.
echo ^ ^ HELP          Shows this menu.
echo ^ ^ INSTALL       Installs the extensions needed for code to run.
echo ^ ^ MAKE          Creates a new project.
echo ^ ^ name          Indicates the project name.
echo ^ ^ UPDATE        Updates and resets a project's conf file to the latest version.